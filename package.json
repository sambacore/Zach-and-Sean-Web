{
  "name": "zach-and-sean-vs-cops",
  "version": "1.0.0",
  "description": "Zach And Sean VS Cops — browser port",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  "dependencies": {
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.4.5",
    "vite": "^5.2.11"
  }
}
